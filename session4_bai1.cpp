#include <stdio.h>

int main(){
	int number;
	    printf("nhap mot so nguyen bat ky:");
	    scanf("%d",&number);
	    if( number > 0 ){
	    	printf("%d la mot so nguyen duong",number);
		}else{
			printf("%d la mot so nguyen am",number);
		}
	
return 0;
}
